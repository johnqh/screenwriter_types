// Ids are owned by writing_core (R1); re-exported so consumers import from one place.
export { ID_PREFIXES, newId, idKind, isId } from '@sudobility/writing_core';
export type { IdPrefix, Id } from '@sudobility/writing_core';
